services:
  db:
    image: postgres:15-alpine
    container_name: loby_local_db
    restart: always
    environment:
      POSTGRES_PASSWORD: my_super_secret_password
      POSTGRES_DB: arcade_db
    ports:
      - '5433:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: